#include "main.h"

/**
 * add - Adds the value of two integers.
 *
 * @a: First integer.
 * @b: Second integer.
 * Return: The result of the addition.
 */
int add(int a, int b)
{
	int ans;
	ans = a + b;
	return (ans);
}
