#include "main.h"

/**
 * mod - Returns the modulus of two integers.
 *
 * @a: First integer.
 * @b: Second integer.
 * Return: The result of the modulus.
 */
int mod(int a, int b)
{
	int ans;
	ans = a % b;
	return (ans);
}
