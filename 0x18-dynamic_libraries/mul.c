#include "main.h"

/**
 * mul - Multiplies the value of two integers.
 *
 * @a: First integer.
 * @b: Second integer.
 * Return: The result of the multiplication.
 */
int mul(int a, int b)
{
	int ans;
	ans = a * b;
	return (ans);
}
