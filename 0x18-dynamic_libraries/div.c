#include "main.h"

/**
 * div - Divides the value of two integers.
 *
 * @a: First Integer.
 * @b: Second integer.
 * Return: The result of the division.
 */
int div(int a, int b)
{
	int ans;
	ans = a / b;
	return (ans);
}
